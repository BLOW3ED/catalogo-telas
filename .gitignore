# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# env — NUNCA subir llaves reales (.env.example SÍ se versiona: es la plantilla sin llaves)
.env
.env.local
.env*.local

# fotos fuente de ingesta (pesadas; viven localmente y en el bucket `telas`)
/FOTOS_TELAS/
/Fotos_Entrada/
/Fotos_Entrada_listas/
/0Fotos_nobg/
*.jpg
*.jpeg
*.png
*.webp
*.bak
!/public/**

# manifest de revisión generado por la ingesta
catalog-manifest.csv
catalog-manifest.auto.json
revision-lote.html
respaldo-bd-*.json
snapshot-catalogo-*.csv


# salida de la skill graphify (grafo de conocimiento del repo, se regenera)
/graphify-out/

# misc
.DS_Store
*.pem
/coverage
*.tsbuildinfo
